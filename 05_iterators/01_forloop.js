// for 
for (let index = 0; index < 10; index++) {
    const element = index;
  //  console.log(element)
    
}
for (let i = 1; i <= 10; i++) {
    const element = i;
    console.log(`Outer loop value: ${i}`)
    for (j = 1; j <=10; j++) {
        const element = j
        console.log(`${i} * ${j} =`+ i*j );
        
    }
}

// break and continue 
// break , breaks the loop and cotinue skip the matching iteration 
//and below code and by incrementing continue the loop 

